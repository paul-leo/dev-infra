# Caddy is NOT used in the default HTTP-only setup.
# Kept here for reference if you later need HTTPS / reverse proxy.
#
# To re-enable: add the caddy service back to docker-compose.yml and
# replace these commented blocks with your actual config.
#
# {
#   local_certs
# }
#
# gitlab.example.com {
#   tls internal
#   reverse_proxy gitlab:80
# }
#
# bit.example.com {
#   tls internal
#   reverse_proxy bit:3000
# }
